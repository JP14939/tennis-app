node_modules/
.env
.env.local
.DS_Store
*.log
__pycache__/
*.pyc
venv/
dist/
build/
.vscode/
.idea/

# Pipeline data output (12GB+, not suited for git without LFS) — see HANDOVER.md
/data/
# Pipeline data / one-off dumps that escaped into the scripts tree
/scripts/data/
/scripts/_baseline4_out.json

# Pretrained COCO weights auto-downloaded by ultralytics on first use (not
# fine-tuned, not original work, redownload automatically wherever missing)
yolo11n.pt
yolo11n-pose.pt

# Cached pose extractions used by tune_rally_gap.py's sweep -- large,
# regenerable from the source video, not meant to be shared/committed
scripts/11_highlight_clipping/.tuning_cache/
