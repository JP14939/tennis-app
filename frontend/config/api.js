// Dev machine's LAN IP so both the phone (Expo Go) and the browser on this
// machine can reach the backend. Update this if your laptop's IP changes
// (check with `ipconfig` / `ifconfig`) — Wi-Fi networks often reassign it.
export const API_BASE = 'http://192.168.1.162:5000';
